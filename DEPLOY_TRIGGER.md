# Cloudflare deployment trigger

This file exists only to trigger a fresh Cloudflare Workers Build from the current `main` revision after the worker status-response syntax fix.

The production worker remains paper-only and real trading remains locked.
