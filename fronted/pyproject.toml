[project]
name = "ai-multi-agent-trading-cloudflare"
version = "1.0.2"
description = "Lightweight Python Worker API for the AI trading dashboard on Cloudflare Workers"
requires-python = ">=3.13"
dependencies = [
    "workers-py>=1.9",
    "workers-runtime-sdk",
]

[dependency-groups]
dev = [
    "workers-py>=1.9",
    "workers-runtime-sdk",
]
