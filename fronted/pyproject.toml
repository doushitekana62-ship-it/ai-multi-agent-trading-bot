[project]
name = "ai-multi-agent-trading-cloudflare"
version = "1.0.0"
description = "FastAPI validation runtime for the AI trading dashboard on Cloudflare Workers"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
