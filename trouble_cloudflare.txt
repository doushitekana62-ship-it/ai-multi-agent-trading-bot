CLOUDFLARE DEPLOYMENT TROUBLE RECORD
====================================

Purpose
-------
Permanent record of recurring Cloudflare deployment regressions. This file is the deployment checkpoint. Do not restart troubleshooting by randomly changing architecture or dependency versions.

CLOUDFLARE BUILD ENVIRONMENT — 2026-08-31
------------------------------------------
- Repository: doushitekana62-ship-it/ai-multi-agent-trading-bot
- Branch: main
- Frontend root: fronted
- Cloudflare build command: bun run build
- Cloudflare automatically runs bun install first.
- Current environment: Node.js 20.20.2 and Bun 1.2.15.
- Python package installation succeeds.

HISTORICAL MUI INCIDENTS
------------------------
1. Bun dependency resolution failed on @mui/icons-material 5.14.20.
2. Bun dependency resolution failed on @mui/utils and @mui/private-theming 5.18.0.
3. CRA/Webpack failed on elementAcceptingRef with tested later MUI 5.x versions.
4. CRA/Webpack failed on chainPropTypes default export.
5. A compatibility patch was introduced and expanded from a few files to 66 and then 912 MUI JS files.
6. The patch merely moved the failure to refType and then HTMLElementType. This proved that patching MUI node_modules files is not a valid solution.
7. MUI 5.17.1 installed successfully, but CRA still failed on MUI utility default exports.

LATEST CLOUDLFARE ERROR — 2026-08-31 05:01 UTC
-----------------------------------------------
Cloudflare successfully reached the frozen environment and installed:
- Node 20.20.2
- Bun 1.2.15
- @mui/material 5.17.1
- @mui/icons-material 5.17.1
- 1296 packages
- Python worker package

Build contract passed and correctly confirmed no MUI node_modules patching. CRA then failed again with:
Attempted import error: '@mui/utils/chainPropTypes' does not contain a default export (imported as 'chainPropTypes').

This proves the remaining problem is not the build contract or Cloudflare environment. The MUI 5.17.1 graph itself is incompatible with the frozen CRA 5/Webpack compilation path used by this repository.

REFERENCE RESEARCH — 2026-08-31
-------------------------------
Web research found MUI's own changelog documenting a @mui/utils module/export change around 5.15.7. The MUI 5.14.0 package dependency graph is therefore the safer compatibility baseline for this legacy CRA/Webpack application.

The documented 5.14.0 graph is:
- @mui/material 5.14.0
- @mui/icons-material 5.14.0
- @mui/system 5.14.0
- @mui/utils 5.13.7
- @mui/private-theming 5.13.7
- @mui/styled-engine 5.13.2
- @mui/core-downloads-tracker 5.14.0
- @mui/types 7.2.4
- @mui/base 5.0.0-beta.7

FIX APPLIED — 2026-08-31
------------------------
Commit 91ea90ebb51acc563a3dc1bf69f4fd44bf14cca1 freezes fronted/package.json to the complete MUI 5.14-compatible dependency graph instead of allowing transitive MUI packages to float to 5.17.x.

Commit 7c35c96dd6da1cbd9fdb08a6333f52e679d4ea2a updates verify-build-contract.js to validate the complete graph. The verifier now fails early if any MUI package drifts.

Browserslist duplicate configuration was fixed in commit e72641ad1e9863d30b52f22c137989e9d50507cb by removing fronted/.browserslistrc, leaving package.json as the sole Browserslist configuration.

STRICT PREVENTION RULES
-----------------------
- Never patch files under node_modules/@mui.
- Never add one-off export patches for chainPropTypes, elementAcceptingRef, refType, HTMLElementType, or another MUI utility.
- Never change one MUI package independently while leaving its transitive MUI graph floating.
- Never use MUI 5.15.7+ for this frozen CRA 5/Webpack baseline unless the frontend is deliberately migrated away from CRA.
- Never run a second package manager after Bun install.
- Never run npm audit fix --force during deployment troubleshooting.
- Never change trading architecture to solve a frontend dependency error.
- Keep Node 20 for CRA 5 unless the project deliberately migrates away from CRA.

ARCHITECTURE PROTECTION
-----------------------
Cloudflare frontend troubleshooting must not alter the trading architecture, INDODAX real-market data requirement, Market Pulse 30-minute rolling window / one-minute segments, AI agent pipeline, Durable Object manual paper-trading STOP/START control, or real-exchange lock.

VALIDATION ORDER
----------------
1. bun install succeeds.
2. Complete MUI 5.14 compatibility graph resolves exactly.
3. build-contract passes.
4. No node_modules MUI patch executes.
5. Browserslist has one configuration source.
6. react-scripts build succeeds.
7. Worker/static asset deployment succeeds.
8. Verify application health/API and existing trading architecture.
